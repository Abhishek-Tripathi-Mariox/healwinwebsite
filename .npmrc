; React 19 + CRA (react-scripts 5) and a few UI libs (react-day-picker 8)
; still declare React <=18 peers. Their resolutions are compatible at runtime,
; so accept legacy peer ranges instead of failing every install.
legacy-peer-deps=true
